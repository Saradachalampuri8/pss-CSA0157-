#include<stdio.h>
int main()
{
    int reverse=0,r,n;
    printf("enter a number: ");
    scanf("%d",&n);
    while(n!=0)
    {
        r=n%10;
        reverse=reverse*10+r;
        n=n/10;
    }
    printf("reversed of a number: %d",reverse);
    return 0;
}
